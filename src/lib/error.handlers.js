
// Simple Toast Handler (can be expanded)
export const handleImageError = (error) => {
    console.error('Profile image error:', error);
    // In a real app, this would trigger a UI toast
    // alert('Profile photo upload failed: ' + error.message); 
};
